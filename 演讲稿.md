# 演讲稿：AI 优先 ≠ 使用 AI

> **使用说明**
> - 每一节对应网页上的一个区块
> - `【滚动】` 表示此时滚动网页到下一屏
> - `【指向屏幕】` 表示示意观众看某个位置
> - 预计总时长：30 分钟（含讨论）

---

## 🖥 封面

最近读了[一篇文章](https://x.com/intuitiveml/status/2043545596699750791)，观点不一定全对，但有些角度挺值得想想。

【滚动】

---

## 🖥 内容区 1 · AI辅助 vs AI优先

大多数团队其实都停在同一个地方——这是文章的起点。

这篇文章叫《Why Your "AI-First" Strategy Is Probably Wrong》，是 CREAO 的联合创始人写的。他是前 Meta LLaMA 团队的成员，物理学博士，公司 25 个人，做 AI 智能体平台。

文章的核心观点是：绝大多数声称"AI优先"的团队，其实只是"AI辅助"。

【指向屏幕左栏】

我们平时所说的用 AI，基本上都是左边这一列：打开 Cursor 生成代码，用 ChatGPT 写需求文档，QA 尝试用 AI 生成测试用例。工具用上了，但工作流程本身没有变。这叫 AI 辅助，效率提升大概在 10% 到 20%。

【指向屏幕右栏】

AI 优先是另一回事。是以 AI 作为主要构建者为前提，把流程、架构、组织全部重新设计。工程师不再是主要写代码的人，而是提供方向和判断。

这两者的差距，不是加法，是乘法。

还有一种常见的情况，叫"氛围编程（vibe coding）"——打开 Cursor，不断对话直到代码能跑，提交，周而复始。这个方式能出原型，但出不了生产系统。生产系统需要稳定、可靠、安全，需要一套体系来保证这些属性。提示词是一次性的，那套体系才是真正的资产。

顺带一提：BCG（波士顿咨询）今年的调研发现，60% 的企业反馈 AI 投入的实际回报极小甚至为零。不是技术问题，是用法没变。

【滚动】

---

## 🖥 分隔屏 1 · 那应该怎么做？

那知道了这个区别之后，具体应该怎么做？

【滚动】

---

## 🖥 内容区 2 · 驾驭工程

OpenAI 在今年二月给这件事起了一个名字，叫"驾驭工程"，Harness Engineering。

核心意思是：工程团队的首要工作，不再是写代码，而是构建让 AI 能够可靠工作的脚手架。出了问题，不是让 AI 再努力一点，而是问——缺少什么能力，如何让智能体能够理解和执行？

【指向屏幕四个卡片】

具体来说是四件事。

第一，测试基础设施。如果不能自动测试智能体写的代码，就不能信任它写的代码。

第二，可观测性。每个服务要输出结构化的、可查询的信号。AI 读不了日志，就没有办法诊断问题。

第三，确定性的部署流水线。CI/CD 不是为了拖慢速度，是为了让流水线的结果可以被预测。流水线可预测，智能体才能推理失败原因。

第四，功能开关。每个功能发布前放在开关后面，出了问题随时可以回滚。风险可控，才敢让 AI 快速迭代。

这四件事是基础设施层面的工作，和业务代码无关，但决定了业务代码是否可靠。

值得一提的是，写《重构》的 Martin Fowler 在同一时期也写了一篇文章，核心观点一样：当 AI 成为主要的代码编写者，工程师的工作就从写代码变成了构建让 AI 可靠工作的基础设施——测试、审查、部署、监控。他把这件事也叫 Harness Engineering。

这四件事不是各自独立的。组合起来之后，会形成一个每天自动运转的循环：

【指向屏幕列表】

每天早上 AI 自动巡检所有服务的错误日志，自动归类、自动创建工单，连重复的问题都会自动去重。工程师推修复后，系统自动验证——问题解决了，工单自动关单。人在整个循环里只做一件事——决定"这个问题要不要现在修、修到什么程度、会不会影响线上用户"。

【滚动】

---

## 🖥 分隔屏 2 · 那我们前端呢？

这是文章给出的框架。把它拿到我们前端来看，会发现什么？

【滚动】

---

## 🖥 内容区 3 · 前端团队的三个问题

把上面这个框架映射到前端，有三个问题值得讨论。

**第一个问题：我们的项目对 AI 友好吗？**

文章里有一个关键的架构决策：把分散在多个仓库的代码统一成 Monorepo，原因只有一个——让 AI 能看到全貌。分散的代码库对智能体来说是不可见的，统一的才是可读的。

映射到我们前端：组件命名是否一致、可预测？样式方案是散落的 CSS 文件，还是有统一的变量体系？状态管理的模式是不是每个项目各写各的？接口对接有没有统一的请求封装和错误处理？目录结构能不能让 AI 不看文档就理解模块边界？

这些本来都是前端工程化的好实践，现在多了一个新的理由：让 AI 能更好地帮我们干活。

**第二个问题：我们的验证能跟上 AI 的产出速度吗？**

文章里说：构建时间两小时，测试时间三天——这不是解决了问题，只是把瓶颈往下游挪了三米。验证必须和实现同速，否则 AI 带来的速度优势会全部堆在 QA 环节消失掉。

但前端的验证有两个难题。逻辑层——状态流转、接口对接、缓存同步、异步竞态——大型项目里这些组合起来的复杂度不亚于后端，现有的测试方案各有短板，没有银弹。视觉层——"这个页面长得对不对"——连写成测试用例都做不到，很大程度上依赖人眼。两层都缺通用解法，这个问题我们后面展开。

**第三个问题：我们的价值往哪里迁移？**

文章的判断是：快速写代码的能力每个月都在贬值，评估、批判、引导 AI 的能力则在升值。

【指向屏幕三个卡片】

从个人技能层面，具体到前端是三件事：

交互判断力——这个交互方案好不好，用户会不会迷失，AI 可以生成代码，但它没有用户感知。

性能系统思维——AI 能写代码，但不会主动做架构级的性能决策，首屏优化、渲染策略、资源加载这些属于系统判断。

AI 工程能力——不只是用 AI 写代码，而是有能力设计让 AI 自主运转的系统：拆任务、给上下文、搭工作流、构建自动化流水线。这就是文章开头说的，从 AI 辅助到 AI 优先。

【滚动】

---

## 🖥 分隔屏 3 · 前端验证的两个难题

前面说了前端验证有两层都难，现在展开说。

【滚动】

---

## 🖥 内容区 4 · 前端验证的两层难题

前端的验证难题分两层，都没有通用解法。如果有，就不需要人盯着 AI 写代码了。

【指向屏幕表格】

先看具体难在哪。

【指向屏幕列表】

逻辑层面：状态在多个组件间共享和联动，一处改动可能引发连锁反应；异步请求的竞态条件——用户连续操作，先发的请求后回来，数据覆盖出错；乐观更新失败后的回滚——UI 已经变了，服务端拒绝了，怎么退回去；mock 和真实 API 行为漂移——测试全绿，上线就炸。

视觉层面：间距差了 4px，功能上没问题，但视觉上就是不对；同一段 CSS 在不同浏览器、不同屏幕尺寸下表现不同；动画的缓动曲线是否舒服，无法写成测试用例；响应式布局在某个中间断点塌了，不试那个宽度根本不知道。

交互层面：滚动吸附是否自然、拖拽动画是否跟手；焦点管理、键盘导航、屏幕阅读器的体验，这些东西连手动测试都容易漏。

状态组合层面：一个表单组件有空状态、填充、错误、禁用、加载、hover、focus……排列组合下来几十种。AI 能生成正常路径的代码，但边缘状态很难全覆盖。

【指向屏幕卡片】

那现有的测试方案呢？

逻辑层：单元测试能覆盖纯函数，但前端大量逻辑绑在组件生命周期里，脱离环境就不真实。集成测试更接近真实场景，但 mock 和真实 API 一旦不同步，测试全绿上线就炸。

视觉层：快照测试比的是 DOM 结构不是视觉效果，改了个 class 名就报错，视觉可能根本没变。视觉回归测试（Chromatic、Percy）做像素级对比，比快照更准，但一个像素变了就标出来，不管有没有意义。

E2E 测试（Playwright、Cypress）比较特殊——它从用户视角操作页面，逻辑层的结果最终体现在 UI 上，所以能同时触及逻辑和视觉两层。但代价是粒度粗、速度慢、维护成本高，大型项目跑一轮几十分钟，而且只能验证最终结果，中间的状态流转和竞态问题它看不到。

每种方案只覆盖一部分。两层加起来，没有哪种方案能让人放心说"AI 写的代码没问题"。

但这个方向有人在认真解决。

【指向屏幕新工具段落】

TestSprite 是目前比较值得关注的一个工具：AI 自主生成端到端测试用例，理解的是组件状态和用户意图，不是像素比对；覆盖视觉回归、响应式断点、无障碍检测，可以通过 MCP 直接集成进 Cursor 工作流，测试失效时会自动修复脆弱的选择器。Chromatic 和 Percy 也在往 AI 感知差异的方向做，减少误报。

说清楚的是：这些工具还在早期，解决的是"像素变了"的检测问题，还没有完全解决"这样对不对"的判断问题。人工审查短期内仍然不可替代，只是可以更精准地知道什么时候需要人眼介入——这是方向，不是现成的答案。

【滚动】

---

## 🖥 分隔屏 4 · 价值迁移

文章最后谈了工程师的价值往哪里走。

【滚动】

---

## 🖥 内容区 5 · 什么不会被替代

文章的判断是：快速写代码的能力在贬值，但架构判断、系统性思维、对"好的"的识别力，这些在 AI 时代反而更稀缺——因为 AI 最难替代的恰恰是这些判断力。

文章作者的原话是："批判 AI 的能力，将比生产代码的能力更有价值。"

文章里还有更多关于角色分工和转型路径的讨论，感兴趣的可以看原文。

【滚动】

---

## 🖥 分隔屏 5 · 想一想

最后，结合这篇文章和我们自己的情况，有三个方向可以想一想。

【滚动】

---

## 🖥 内容区 6 · 三个思考方向

【指向屏幕三张卡片】

第一个：团队的隐性知识，AI 进不了传递链。

举个例子：组件库升级了一半，旧写法和新写法同时存在。老员工知道"新页面用新的"，新人要踩坑才知道，AI 永远不知道——它看到哪个就学哪个。再比如某个接口封装是有原因的，绕过去会触发历史 bug，这件事写在老员工脑子里，没有文件，AI 可能直接调原始接口。

这类知识我们靠"老带新"维持，AI 进不了这个传递链。它读不到的，就等于不存在。

【指向屏幕第二张卡片】

第二个：我们有没有最基本的自动验证？

文章说"驾驭工程"第一条就是测试基础设施——不能自动测试，就不能信任 AI 写的代码。如果我们的项目连基本的自动化测试都没跑起来，那不管 AI 多强，生成的代码对不对，最终全靠人看。这可能是当前最大的瓶颈，也是最值得思考的起点。

【指向屏幕第三张卡片】

第三个：从"用 AI"到"为 AI 而建"。

前两个问题都是具体的切入点，但背后指向同一件事：我们现在是把 AI 加进了已有的工作流，流程本身没有变。文章开头区分的 AI 辅助和 AI 优先，差距不在工具，在于有没有把流程、架构、验证体系围绕 AI 重新设计。这不是今天就要做的事，但值得开始想。

【停顿】

文章的原文、Martin Fowler 的延伸阅读，在网页底部都有链接。

如果大家有什么经验或者想法，欢迎聊聊。
